name: repository
description: Generate a complete repository pattern implementation with data sources, models, and comprehensive testing.

# The following defines the brick repository url.
# Uncomment and update the following line before publishing the brick.
# repository: https://github.com/my_org/my_repo

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.1.0+1

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ^0.1.1

# Variables specify dynamic values that your brick depends on.
# Zero or more variables can be specified for a given brick.
# Each variable has:
#  * a type (string, number, boolean, enum, array, or list)
#  * an optional short description
#  * an optional default value
#  * an optional list of default values (array only)
#  * an optional prompt phrase used when asking for the variable
#  * a list of values (enums only)
#  * an optional separator (list only)
vars:
  name:
    type: string
    description: Your repository name
    default: user
    prompt: What is your repository name?

  has_remote_data_source:
    type: boolean
    description: Include remote data source (API calls)
    default: true
    prompt: Include remote data source?

  has_local_data_source:
    type: boolean
    description: Include local data source (local storage)
    default: true
    prompt: Include local data source?

  model_name:
    type: string
    description: Model name for the repository
    default: "{{name.pascalCase()}}"
    prompt: What is your model name? (leave empty to use repository name)