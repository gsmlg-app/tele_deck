name: screen
description: Create a screen file in lib/screens/ folder with proper navigation and theming integration.

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.1.0+1

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ^0.1.1

# Variables specify dynamic values that your brick depends on.
# Zero or more variables can be specified for a given brick.
# Each variable has:
#  * a type (string, number, boolean, enum, array, or list)
#  * an optional short description
#  * an optional default value
#  * an optional list of default values (array only)
#  * an optional prompt phrase used when asking for the variable
#  * a list of values (enums only)
#  * an optional separator (list only)
vars:
  name:
    type: string
    description: Screen name
    default: Profile
    prompt: What is your screen name?

  folder:
    type: string
    description: Subfolder name (optional, e.g., 'user' for lib/screens/user/)
    default: ""
    prompt: Subfolder name (leave empty for root screens folder)?

  has_adaptive_scaffold:
    type: boolean
    description: Use AppAdaptiveScaffold for responsive layout
    default: true
    prompt: Use AppAdaptiveScaffold?

  has_app_bar:
    type: boolean
    description: Include SliverAppBar in the screen
    default: true
    prompt: Include SliverAppBar?